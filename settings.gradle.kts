plugins {
    // Disabled to avoid toolchain download issues in restricted network environments
    // id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}
rootProject.name = "spector"